# Audit complet RSI — 2026-08-23 (branche `feat/cogno1-completion-v2`, 236 commits)

> **Document de travail** : checklist de remédiation point par point. Cocher
> `[x]` et référencer le commit réel à chaque point traité. Reprise possible à
> tout instant : les points non cochés restent à faire, dans l'ordre.
>
> Règle de remédiation : un commit par point (ou groupe cohérent), tests verts
> (`cargo test --workspace --all-features`) avant chaque push.

## Méthodo & vérifications exécutées

- Lecture de ~100 % du code source (src/, src/bin/, forge/, octasoma/, ccos/,
  crates/, scripts/, CI, Dockerfile, Makefile).
- `cargo build --all-features` OK · `cargo test --workspace --all-features`
  → **495 passed / 0 failed** · `cargo clippy --all-features` → **2 warnings**.
- Greps exhaustifs : 0 TODO/FIXME/placeholder dans le code Rust ; 0 `unsafe`.

## Verdict global

Code remarquablement durci (parseur JSON borné+fuzzé, sous-processus bornés,
dry-run DGM strict, CI réelle). Aucun bug critique bloquant ; 4 défauts
élevés, 12 moyens, une dette stratégique (~10k lignes autopilot non câblées).

---

## 🟠 ÉLEVÉ

- [x] **E1 — `ccos/src/lib.rs` : intégrité trouée.** ✅ commit `4b9a994`
      Le hash de maillon n'incluait ni `event_type` ni un tag de type de
      payload (`Text("a\0b") ≡ Custom{key:"a",value:"b"}`) : altérer
      `EventType` était indétectable par `verify_integrity`. Incohérent avec
      `src/audit.rs:59` qui hache bien event_type. **Fix appliqué** : tag de
      type dans le hash + `EventType::as_str()` + tests de non-altération du
      type et d'absence de collision de variantes.
- [x] **E2 — `src/dgm.rs:764` `promote_to_live` sans garde de chemin.** ✅ commits `8d7ab22` + `ac4e86d`
      La seule fonction qui mute l'arbre vivant n'avait pas la garde du
      snapshot. **Fix appliqué** : canonicalisation + ancrage, ET rejet des
      composants `..`/absolus en amont (canonicalize échoue sur fichier
      inexistant et laissait passer un chemin brut `a/../b`). Garde aussi
      ajoutée à `WorkspaceSnapshot::apply`. Tests d'évasion relatif/absolu,
      victime intacte.
- [x] **E3 — `src/web_crawl.rs:642` `snippet()` panic UTF-8.** ✅ commit `f94e736`
      Bornes de slicing alignées sur char_boundaries (helpers
      `floor/ceil_char_boundary`) + test texte accentué multi-octets.
- [x] **E4 — `forge/src/lib.rs:167` tri par somme d'absolus.** ✅ commit `410273a`
      Clé de classement = somme signée (NaN → +∞), doc corrigée, test
      `signed_objective_sorting`.

## 🟡 MOYEN

- [x] **M1 — `octasoma` contrat `insert()` mensonger + double normalisation.** ✅ commit `c36341a`
      Upsert récursif avec dédoublonnage (position projetée identique →
      payload remplacé, ancien retourné, compteur inchangé) ; normalisation
      simple conforme à la doc ; tests dédoublonnage feuille + arbre profond.
- [x] **M2 — `cross_validate.rs:140` `compare_gradients` ne compare rien.** ✅ commit `8b334c3`
      Nouveau contrat : gradient numérique (différences finies) vs analytique,
      tolérance relative appliquée, `GradientComparison{max_abs_diff, matches}`,
      erreur structurée si longueurs incohérentes + tests.
- [x] **M3 — `web_crawl.rs` crawler : race d'arrêt + robots non cachés.** ✅ commit `87cb4e6`
      Arrêt seulement file vide ET zéro fetch en vol ; robots.txt en cache par
      hôte/session (verrou jamais tenu pendant l'I/O) ; politesse découplée de
      `respect_robots` avec créneau réservé sous verrou ; `visited/skipped/
      errors` alimentés correctement.
- [x] **M4 — `forge/src/lib.rs:217` un seed refusé tue la campagne.** ✅ commit `835cb42`
      Seeds invalides écartés (campagne démarre si ≥1 valide, Err sinon) ;
      champ mort `worker_addresses` retiré + 2 appelants nettoyés.
- [x] **M5 — panics `steps=0`** (`main.rs:112`, `rsi_full.rs:146/230`,
      `rsi_ablate.rs:134`) + division `(end-start)/start_si` sans garde zéro. ✅ commit `6f3ba9e`
      rsi-demo : message gracieux ; rsi-full : early-return + helper `pct()` ;
      rsi-ablate : rejet steps/seeds=0 (exit 2) + plus de unwrap.
- [x] **M6 — `rsi-crawl` export JSONL cassé.** ✅ commit `fd5c469`
      Sérialisation/parsing via `crate::json` des deux côtés ; lignes
      invalides signalées ; field()/esc() supprimés.
- [x] **M7 — Dockerfile : image root, tag flottant, `--help` trompeur.** ✅ commit `4b8a199`
      USER 65534:65534 (scratch = UID numérique), note digest pour
      reproductibilité, doc stdin corrigée.
- [x] **M8 — workflow résiduel `.github/workflows/p2-2-clippy-fix.yml`** ✅ commit `681d33f`
      Supprimé (bot auto-commit contents:write toujours armé).
- [x] **M9 — `api.rs` `Session::history` non bornée.** ✅ commit `0387e67`
      Plafond MAX_HISTORY (100k) en fenêtre glissante via clamp_history(),
      testée à petite échelle ; exports sur la fenêtre conservée.
- [x] **M10 — `adamw.rs` config non validée.** ✅ commit `d080d96`
      AdamWConfig::validate() appelée par AdamW::new -> Result (betas ∈ [0,1),
      lr/eps>0, wd≥0, clip>0) + test rejects_invalid_configs.
- [x] **M11 — `rsi_connect.rs` : config non-JSON écrasée sans backup ;
      écriture non atomique ; exit 0 toujours.** ✅ commit `867c138`
      .bak + refus d'écraser, racine non-objet rejetée, tmpfile+rename,
      exit 1 si 0/N cibles. Vérifié manuellement (fichier cassé intact).
- [x] **M12 — Makefile : défaut=install ; `make ci` omet `scirust` ; pas de
      --locked.** ✅ commit `3e16e6d`
      .DEFAULT_GOAL=help + cible help ; PUBLIC_FEATURES identiques à ci.yml
      (scirust inclus) ; --locked partout.

## 🔵 MINEUR

- [ ] **m1 — `web_crawl.rs:135` port https par défaut 80.** Fix : 443 en https.
- [ ] **m2 — `parse_html:414` substring href** (`data-href` faux positif, 1er
      attribut seulement). Fix : extraction d'attributs aux frontières de mot.
- [ ] **m3 — entités hex `&#x27;` non décodées.** Fix : décodeur hex+décimal.
- [ ] **m4 — `dgm.rs select_parent` ignore score** malgré doc « qualité ×
      nouveauté ». Fix : composante continue issue de la fitness.
- [ ] **m5 — snapshot copie sans borne de taille.** Fix : budget d'octets,
      dépassement = erreur propre.
- [ ] **m6 — `rsi_scholar.rs:281/321` slicing octets d'id** (panic multi-octets
      théorique). Fix : slicing chars.
- [ ] **m7 — `rsi_ascend.rs:161` Duration NaN/négatif panic possible.** Fix :
      validation finie > 0.
- [ ] **m8 — `llm.rs` réponse HTTP lue sans plafond.** Fix : lecture bornée.
- [ ] **m9 — `flake.nix:46` commentaire périmé (« Cargo.lock gitignoré »).**
- [ ] **m10 — `.gitignore` manque `.rsi-refine.json`.**
- [ ] **m11 — warnings clippy --all-features** (`scirust_bridge.rs:17`
      attribut dupliqué ; `web_crawl.rs:394` else-if collapsible) + job CI
      clippy all-features pour ne plus laisser passer.
- [ ] **m12 — CI : timeout-minutes / concurrency manquants.**

## 🏗️ AMÉLIORATIONS (recommandations audit)

- [ ] **A1 — Signature HMAC-SHA256 optionnelle du journal d'audit**
      (`HashChainLog::with_key`) : non-répudiation contre réécriture de chaîne.
- [ ] **A2 — Anti-SSRF crawler** : blocage par défaut hôtes privés/loopback/
      link-local (opt-out explicite).
- [ ] **A3 — robots.txt complet** : cache par hôte (via M3), règles `Allow`
      prioritaires, Crawl-delay lu si présent.
- [ ] **A4 — Consumer minimal de la pile autopilot** : exemple exécutable
      intake→dag→perf→PR plan (la pile ~8k lignes n'a aucun consommable).
- [ ] **A5 — Fuzzing JSON en job nightly CI** (mini-fuzz in-tree existant).
- [ ] **A6 — Domaine WASM branché comme évaluateur alternatif DGM**
      (transport wasmi prêt derrière feature `wasm`).

---

*Audit initial : ce commit. Chaque case cochée référencera son commit réel.*
