# Audit complet RSI — 2026-08-23 (branche `feat/cogno1-completion-v2`, 236 commits)

> **Document de travail** : checklist de remédiation point par point. Cocher
> `[x]` et référencer le commit réel à chaque point traité. Reprise possible à
> tout instant : les points non cochés restent à faire, dans l'ordre.
>
> Règle de remédiation : un commit par point (ou groupe cohérent), tests verts
> (`cargo test --workspace --all-features`) avant chaque push.

## Méthodo & vérifications exécutées

- Lecture de ~100 % du code source (src/, src/bin/, forge/, octasoma/, ccos/,
  crates/, scripts/, CI, Dockerfile, Makefile).
- `cargo build --all-features` OK · `cargo test --workspace --all-features`
  → **495 passed / 0 failed** · `cargo clippy --all-features` → **2 warnings**.
- Greps exhaustifs : 0 TODO/FIXME/placeholder dans le code Rust ; 0 `unsafe`.

## Verdict global

Code remarquablement durci (parseur JSON borné+fuzzé, sous-processus bornés,
dry-run DGM strict, CI réelle). Aucun bug critique bloquant ; 4 défauts
élevés, 12 moyens, une dette stratégique (~10k lignes autopilot non câblées).

---

## 🟠 ÉLEVÉ

- [ ] **E1 — `ccos/src/lib.rs` : intégrité trouée.**
      Le hash de maillon n'inclut ni `event_type` ni un tag de type de payload
      (`Text("a\0b") ≡ Custom{key:"a",value:"b"}`) : altérer `EventType` est
      indétectable par `verify_integrity`. Incohérent avec `src/audit.rs:59`
      qui hache bien event_type. **Fix** : tag de type dans `payload_bytes` +
      `event_type` dans le hash + tests de non-altération du type.
- [ ] **E2 — `src/dgm.rs:764` `promote_to_live` sans garde de chemin.**
      La seule fonction qui mute l'arbre vivant n'a pas la vérification
      canonicalize+starts_with de `WorkspaceSnapshot::apply:741-748`. Un patch
      `target="../x"` sort de la racine. **Fix** : même garde que apply + test
      d'évasion.
- [ ] **E3 — `src/web_crawl.rs:642` `snippet()` panic UTF-8.**
      Slicing octets `&text[start..end]` sur frontière non-char-boundary →
      panic sur texte non-ASCII (mots accentués = cas nominal). **Fix** :
      alignement sur char_boundaries + test multioctet.
- [ ] **E4 — `forge/src/lib.rs:167` tri par somme d'absolus.**
      En minimisation multi-objectif à signes mixtes, un objectif bon (négatif)
      pénalise le classement ; commentaire « somme pondérée » faux.
      **Fix** : somme signée (minimisation correcte) + doc corrigée + test.

## 🟡 MOYEN

- [ ] **M1 — `octasoma` contrat `insert()` mensonger + double normalisation.**
      Doc promet la détection de doublon mais retourne toujours le payload ;
      `len()` compte les doublons ; `project()` divise 2× par la norme
      (`sig(acc/norm)` puis `/norm.max(1)` interne). **Fix** : détection réelle
      du point déjà projeté, normalisation simple, doc alignée, tests.
- [ ] **M2 — `cross_validate.rs:140` `compare_gradients` ne compare rien.**
      Calcule juste un gradient par différences finies et ignore la tolérance
      (`let _ = tolerance`). Contrat §14 non tenu. **Fix** : comparer gradient
      numérique vs analytique fourni, tolérance appliquée, test.
- [ ] **M3 — `web_crawl.rs` crawler : race d'arrêt + robots non cachés.**
      Workers peuvent sortir tant que d'autres vont pousser des liens (crawl
      incomplet non déterministe) ; robots.txt retéléchargé à CHAQUE page ;
      politesse couplée au flag respect_robots ; `skipped` jamais alimenté ;
      erreurs comptées comme visites (:810). **Fix** : suivi actifs/en attente,
      cache robots par hôte, découplage politesse, compteurs corrects.
- [ ] **M4 — `forge/src/lib.rs:217` un seed refusé tue la campagne.**
      `collect::<Result<Vec<_>>>()?` abort tout au lieu de re-échantillonner ;
      champ `worker_addresses` mort. **Fix** : skip/retry des seeds invalides,
      champ mort retiré (breaking mineur documenté).
- [ ] **M5 — panics `steps=0`** (`main.rs:112`, `rsi_full.rs:146/230`,
      `rsi_ablate.rs:134`) + division `(end-start)/start_si` sans garde zéro.
      **Fix** : message propre + exit 1, garde division.
- [ ] **M6 — `rsi-crawl` export JSONL cassé.** `esc()` n'échappe ni `\n` ni
      `\t` ni contrôles <0x20 (JSONL invalide, ré-indexation corrompue) ;
      extracteur `field()` casse sur `\"`. **Fix** : sérialiser/parser via
      `crate::json` (déjà disponible).
- [ ] **M7 — Dockerfile : image root, tag flottant, `--help` trompeur.**
      **Fix** : `USER 65534`, pin digest documenté, commentaire corrigé.
- [ ] **M8 — workflow résiduel `.github/workflows/p2-2-clippy-fix.yml`**
      (bot auto-commit avec `contents: write`). **Fix** : suppression.
- [ ] **M9 — `api.rs` `Session::history` non bornée** (MAX_STEPS borne par
      appel, pas le cumul). **Fix** : plafond MAX_HISTORY fenêtre glissante,
      export intact, test.
- [ ] **M10 — `adamw.rs` config non validée** (`beta1=1.0` → div/0 dans
      `1-b1^t` ; lr/eps libres). **Fix** : validation explicite + test.
- [ ] **M11 — `rsi_connect.rs` : config non-JSON écrasée sans backup ; écriture
      non atomique ; exit 0 même si 0/N cibles.** **Fix** : `.bak` + tmp+rename
      + exit 1 si aucun succès.
- [ ] **M12 — Makefile : défaut=install ; `make ci` omet `scirust` vs CI ;
      pas de --locked.** **Fix** : .DEFAULT_GOAL=help, cible help(), ci aligné
      features+scirust, --locked partout.

## 🔵 MINEUR

- [ ] **m1 — `web_crawl.rs:135` port https par défaut 80.** Fix : 443 en https.
- [ ] **m2 — `parse_html:414` substring href** (`data-href` faux positif, 1er
      attribut seulement). Fix : extraction d'attributs aux frontières de mot.
- [ ] **m3 — entités hex `&#x27;` non décodées.** Fix : décodeur hex+décimal.
- [ ] **m4 — `dgm.rs select_parent` ignore score** malgré doc « qualité ×
      nouveauté ». Fix : composante continue issue de la fitness.
- [ ] **m5 — snapshot copie sans borne de taille.** Fix : budget d'octets,
      dépassement = erreur propre.
- [ ] **m6 — `rsi_scholar.rs:281/321` slicing octets d'id** (panic multi-octets
      théorique). Fix : slicing chars.
- [ ] **m7 — `rsi_ascend.rs:161` Duration NaN/négatif panic possible.** Fix :
      validation finie > 0.
- [ ] **m8 — `llm.rs` réponse HTTP lue sans plafond.** Fix : lecture bornée.
- [ ] **m9 — `flake.nix:46` commentaire périmé (« Cargo.lock gitignoré »).**
- [ ] **m10 — `.gitignore` manque `.rsi-refine.json`.**
- [ ] **m11 — warnings clippy --all-features** (`scirust_bridge.rs:17`
      attribut dupliqué ; `web_crawl.rs:394` else-if collapsible) + job CI
      clippy all-features pour ne plus laisser passer.
- [ ] **m12 — CI : timeout-minutes / concurrency manquants.**

## 🏗️ AMÉLIORATIONS (recommandations audit)

- [ ] **A1 — Signature HMAC-SHA256 optionnelle du journal d'audit**
      (`HashChainLog::with_key`) : non-répudiation contre réécriture de chaîne.
- [ ] **A2 — Anti-SSRF crawler** : blocage par défaut hôtes privés/loopback/
      link-local (opt-out explicite).
- [ ] **A3 — robots.txt complet** : cache par hôte (via M3), règles `Allow`
      prioritaires, Crawl-delay lu si présent.
- [ ] **A4 — Consumer minimal de la pile autopilot** : exemple exécutable
      intake→dag→perf→PR plan (la pile ~8k lignes n'a aucun consommable).
- [ ] **A5 — Fuzzing JSON en job nightly CI** (mini-fuzz in-tree existant).
- [ ] **A6 — Domaine WASM branché comme évaluateur alternatif DGM**
      (transport wasmi prêt derrière feature `wasm`).

---

*Audit initial : ce commit. Chaque case cochée référencera son commit réel.*
